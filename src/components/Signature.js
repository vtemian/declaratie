import styled from "styled-components";

import { color, borderRadius } from "../helpers/constants";

const Signature = styled.div`
  border: 1px solid ${color.black};
  border-radius: ${borderRadius.large};
`;

export default Signature;
